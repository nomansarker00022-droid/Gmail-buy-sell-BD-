# Project Rules & Directives

- **Strict Explicit Request Requirement**: Do NOT make any automatic changes, layout modifications, or feature updates unless explicitly requested by the user in a prompt.
- **Top Header & Meta Theme Color**: Keep the browser `theme-color` meta tag consistently blue (`#1D4ED8`).
- **Menu Icon**: Use the 3 horizontal lines (`Menu` icon) for the top-right header menu button.
